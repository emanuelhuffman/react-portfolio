import React from "react";
import Card from "react-bootstrap/Card";

const Block = () => {
  return (
    <Card
      style={{
        background: "red",
        width: "200px",
        height: "200px",
      }}
    >
      Block
    </Card>
  );
};

export default Block;
