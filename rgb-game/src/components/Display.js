import React from "react";
import Container from "react-bootstrap/Container";

const Display = () => {
  return (
    <Container style={{ background: "aqua", height: "10rem" }} fluid>
      Display
    </Container>
  );
};

export default Display;
